image: maven_3-jdk-8

stages:
    - build

maven-build:
    stage: build
    script: "mvn package"
    artifacts:
        reports:
            junit:
                - target/surefire-reports/TEST-*.xml
